#include "EndpointReferenceType.hpp"

class From:public EndpointReferenceType 
{
      /***
       * Constructor From
       * 
       * @param address 
       */
public:
    From();
    From(AxisChar * address);   
  };
